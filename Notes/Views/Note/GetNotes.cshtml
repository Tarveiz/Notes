@model List<Notes.Domain.ViewModel.Note.NoteViewModel>

@{
    ViewData["Title"] = "Notes";
    int rowCount = 0;
}
<form method="get" asp-antiforgery="true">
        <div class="table">
            <div class="table-head">
                <div class="table-th">№</div>
                <div class="table-th table-th-second">Имя заметки</div>
                <div class="table-th">Описание</div>
                <div class="table-th table-th-second">Дата создания</div>
            </div>
            <div class="table-body">
                @foreach (var note in Model)
                {
                rowCount++;
                <div class="table-row">
                    <div class="table-cell">@rowCount</div>
                        <div class="table-cell">
                            <span>@note.Name</span>
                            <div class="buttons">
                                <a asp-action="CreateNote" asp-route-id="@note.Id"><img src="~/images/258332.png" /></a>
                                <a asp-action="DeleteNote" asp-route-id="@note.Id"><img src="~/images/3299937.png" /></a>
                            </div>
                        </div>
                        <div class="table-cell">@note.Description</div>
                        <div class="table-cell">@note.Date</div>
                </div>
                }
                <div class="table-bottom">
                    <div class="table-row">
                        <div class="table-cell"><a asp-controller="Note" asp-action="CreateNote" class="table-create-button" ><img src="~/images/5379016.png" /></a></div>
                        <div class="table-cell"></div>
                        <div class="table-cell"></div>
                        <div class="table-cell"></div>
                    </div>
                </div>
            </div>
        </div>
        </form>
@*}*@




